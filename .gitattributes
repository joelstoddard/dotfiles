# Strips the session state Claude Code writes here. Inert until the filter is
# registered in git config, which home/git.nix does — see
# docs/design/claude-settings-split.md
.claude/user-settings.json filter=claude-settings
