# Strips the session state Claude Code writes here. Inert until
# scripts/install.py::configure_settings_filter registers the filter — see
# docs/design/claude-settings-split.md
.claude/user-settings.json filter=claude-settings
